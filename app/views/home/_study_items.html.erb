<div class="columns">
    <% study_items.each do |study_item|  %>
        <div class="container">
            <div class="column notification is-primary">
                <p class="subtitle is-centered"><%= study_item.full_title %></p>
                <p class="subtitle"><%= study_item.title %></p>
                <%= link_to 'X', new_study_item_path, class: "is-right button is-danger" %>
            </div>
    <% end %>
        </div>
<br/>
</div>